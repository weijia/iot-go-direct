16区指令板和控制板串口通信协议
 
文档信息
 
项目 内容 
通信双方 网关 ↔ 通讯板（指令板/控制板） 
数据包总长 固定 11 字节 
协议类型 串口定长通信协议 
编制日期 2026年07月 
 
一、数据包通用整体结构
 
所有上行、下行数据包均为11字节定长结构，字节序号 1~11。
 
字节序号 字段说明 
第1字节 固定帧头 
第2字节 指令/应答状态码(高4bit) + 有效数据长度(低4bit) 
第3~10字节 8字节负载域，一字节控制2个分区，共16个分区 
第11字节 累加校验字节（校验范围：第1字节~第10字节） 
 
长度固定为8：指代第3字节至第10字节的有效负载长度恒为8，不包含帧头与校验位。
 
二、网关 → 通讯板 下行下发协议
 
2.1 逐字节字段定义
 
字节位 字段名称 详细说明 
1 帧头 固定值  0x5A ，帧起始标志 
2 指令码(高4bit) + 长度(低4bit) 指令枚举： 1 = 改色指令 2 = 状态回报指令 低4位长度固定为8 
3 01区、02区颜色控制 单字节高低4位分别控制2个分区颜色 
4 03区、04区颜色控制 同上 
5 05区、06区颜色控制 同上 
6 07区、08区颜色控制 同上 
7 09区、10区颜色控制 同上 
8 11区、12区颜色控制 同上 
9 13区、14区颜色控制 同上 
10 15区、16区颜色控制 同上 
11 校验字节 第1~10字节累加和取低8位 
 
2.2 颜色控制值定义
 
数值 分区颜色含义 
0 透明 
2 浅色 
4 深色 
F 保留原有颜色，不做变更 
 
2.3 下发阻塞规则
 
16个分区中任意一个分区处于上电中状态时，通讯板会直接忽略网关所有下发指令，不会执行也不会回复应答包；
直到所有分区上电流程全部结束后，通讯板才恢复接收网关指令。
 
三、通讯板 → 网关 上行应答协议
 
3.1 逐字节字段定义
 
字节位 字段名称 详细说明 
1 帧头 固定值  0x5A  
2 应答状态码(高4bit) + 长度(低4bit) 状态码枚举： 0 = 指令错误 1 = 改色指令正常响应 2 = 回报指令正常响应 低4位长度固定为8 
3 01区、02区状态上报 单字节高低4位反馈2个分区运行状态 
4 03区、04区状态上报 同上 
5 05区、06区状态上报 同上 
6 07区、08区状态上报 同上 
7 09区、10区状态上报 同上 
8 11区、12区状态上报 同上 
9 13区、14区状态上报 同上 
10 15区、16区状态上报 同上 
11 校验字节 第1~10字节累加和取低8位 
 
3.2 应答状态码业务逻辑
 
1. 网关指令解析出错：状态码返回  0 ，第3~10字节全部填充  0xFF ；
2. 收到合法改色指令(指令1)：状态码返回  1 ，负载上报分区实时状态；
3. 收到合法回报指令(指令2)：状态码返回  2 ，负载上报分区实时状态；
 
3.3 分区运行状态编码定义
 
编码 状态含义 
A 已完成 
B 执行中 
C 上电中 
F 保留，状态不变 
 
四、校验算法规范
 
1. 参与校验范围：上行包、下行包均使用「第1字节 ~ 第10字节」参与累加计算；
2. 计算规则：对所有参与字节无符号累加，最终结果与  0xFF  做按位与运算，截取低8位作为第11字节校验值；
3. 接收校验逻辑：接收方计算前10字节校验和，与数据包第11字节比对，不一致则判定数据包异常，整包丢弃。
 
五、异常处理规则
 
1. 帧头不等于  0x5A ：直接丢弃数据包，无应答；
2. 校验字节校验失败：丢弃数据包，无应答；
3. 指令码非1/2有效范围：判定指令错误，回复状态码0，负载全部为 0xFF ；
4. 存在分区上电中(C态)：网关下发所有指令全部阻塞丢弃，无应答；
5. 数据包长度不等于11字节：判定粘包/断包，直接丢弃。
 
六、交互业务流程
 
6.1 改色指令交互（网关下发指令1）
 
1. 网关组装11字节改色数据包完成串口下发；
2. 通讯板校验帧头、校验码，同时检查是否存在上电中分区；
3. 存在上电分区：丢弃数据包，无应答；
4. 无阻塞：执行分区改色动作，返回状态码=1的应答包，上报各分区实时状态。
 
6.2 状态回报指令交互（网关下发指令2）
 
1. 网关下发回报指令数据包；
2. 通讯板校验数据包合法后，不执行改色动作，直接返回状态码=2的应答包，上报16分区完整运行状态。
 
6.3 非法指令交互
 
通讯板回复状态码0，第3~10字节填充全 0xFF ，告知网关指令解析失败。
 
